index=my_app_index met
| rex "cpuUsage\":(?<cpu_usage>[\d.]+)"
| rex "freeHeap\":\s*(?<free_heap>[\d.]+)"
| eval cpu_pct = round(cpu_usage * 100, 2)
| timechart span=1m avg(cpu_pct) as "CPU %" avg(free_heap) as "Free Heap MB"